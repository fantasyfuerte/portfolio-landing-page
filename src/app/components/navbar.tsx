const Navbar = () => {
  return (
    <div className="fixed flex flex-col items-center justify-center w-full mt-auto h-max bottom-10">
      This text is in the navbar
    </div>
  );
};

export default Navbar;

// 20m 6:15pm
